# 开发日志 - 2026-06-17

## 标签增强功能（v2 后新增）

### 自动补全
- [x] 修改 `Views/TagInputView.swift`，输入时在下方展示匹配的已有标签
- [x] 合并输入框和建议列表为统一胶囊形容器（RoundedRectangle cornerRadius 25）
- [x] 点击建议项直接添加标签，无需回车
- [x] 新标签名无匹配时回车新建（保持原有行为）

### 标签重命名
- [x] `ClipRepository` 新增 `renameTag(from:to:)` — 批量更新所有带旧标签的记录
- [x] `ClipHistoryViewModel` 新增 `renameTag(from:to:)` — 调用 repo 后刷新 UI
- [x] `HistoryPanelView` 标签栏支持双击原地编辑，回车确认，Esc 取消
- [x] 重命名后自动刷新标签列表，如果当前正在按旧标签筛选则自动切换到新标签

### Swift 6 兼容性
- [x] `Package.swift` 添加 `.swiftLanguageMode(.v5)` 解决 Swift 6 严格并发编译错误
- [x] `App/main.swift` 使用 `MainActor.assumeIsolated`
- [x] `ClipboardMonitor.swift` 使用 `nonisolated(unsafe)` 修复 db 指针警告

### 变更文件
| 文件 | 操作 |
|------|------|
| `Views/TagInputView.swift` | 自动补全 + 合并容器 |
| `Views/HistoryPanelView.swift` | 标签双击重命名 UI |
| `ViewModels/ClipHistoryViewModel.swift` | +renameTag 方法 |
| `Services/ClipRepository.swift` | +renameTag 方法 |
| `App/main.swift` | MainActor.assumeIsolated |
| `Services/ClipboardMonitor.swift` | nonisolated(unsafe) |
| `Package.swift` | .swiftLanguageMode(.v5) |
